# ============================================================
# TELERADAR-PGO — Variáveis de Ambiente
# Copie este arquivo para .env e preencha os valores reais
# NUNCA commite o arquivo .env no repositório
# ============================================================

# BANCO DE DADOS
# No Render, esta variável é preenchida automaticamente via render.yaml
DATABASE_URL=postgresql+asyncpg://user:password@host:5432/teleradar_pgo

# SEGURANÇA JWT
# Gerar com: openssl rand -hex 32
SECRET_KEY=troque-por-um-secret-gerado-com-openssl-rand-hex-32
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# AMBIENTE
ENVIRONMENT=development
# Valores: development | production

# EMAIL SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu@email.com
SMTP_PASSWORD=sua_senha_de_app_google
ADMIN_MASTER_EMAIL=admin@teleradar.com.br

# GOOGLE OAUTH2 (opcional)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:8000/auth/google/callback

# RATE LIMIT
MAX_LOGIN_ATTEMPTS=5
LOCK_DURATION_MINUTES=15

# BOOTSTRAP DO USUÁRIO MASTER
# Gerar com: openssl rand -hex 16
# Após criar o MASTER, pode remover esta variável ou deixar vazia
BOOTSTRAP_SECRET=troque-por-um-secret-seguro

# CORS (separar por vírgula)
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# RENDER FREE TIER — Monitoramento
# TODO:UPGRADE: Estas variáveis controlam alertas do Free Tier
DB_CREATED_AT=2026-02-21
DB_STORAGE_ALERT_MB=800
